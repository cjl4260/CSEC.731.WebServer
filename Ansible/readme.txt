
Utilize this command to run the playbook:
    ansible-playbook deploy-web-server.yaml

Make sure to run this command from the directory where the playbook file is located, or provide the full path to the file in the command. 
Also, make sure that your inventory file is correctly configured with the hostname of the server you want to deploy the web server on.
